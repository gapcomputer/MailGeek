{
  "name": "mailgeek-chrome-extension",
  "version": "1.0.0",
  "description": "MailGeek Chrome Extension",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest watch",
    "test:coverage": "vitest run --coverage",
    "build": "babel src --out-dir dist",
    "prepare": "babel --copy-files --out-dir dist src"
  },
  "devDependencies": {
    "@babel/core": "^7.22.9",
    "@babel/preset-env": "^7.22.9",
    "jsdom": "^26.1.0",
    "vitest": "^0.33.0"
  },
  "dependencies": {
    "jquery": "^3.7.1"
  }
}
